# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# AIZynthFinder + REINVENT virtualenv (8.1G) - regenerable, and files exceed
# GitHub's 100MB/file hard limit, so it must never be committed.
project/venvs/

# Public AIZynthFinder model/stock files (663MB ZINC stock + ONNX weights),
# regenerated via `download_public_data`. Tracked by Git LFS if ever committed.
project/synth_route/models/